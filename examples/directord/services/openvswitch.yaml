id: openvswitch
type: service
version: 1.0.0
tasks:
  - id: init
    action: run
    driver: directord
    provides:
      - openvswitch.init
    requires:
      - repos.init
    jobs:
      - DNF: openvswitch
      - SERVICE: --restarted --enable openvswitch

